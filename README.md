# desk_timer

NOTE: For some reason, I needed to add "-isystem /usr/arm-none-eabi/include/c++/14.2.0" to the `compile_commands.json` entry for `main.cxx` in order for `clangd` to find the system includes.
